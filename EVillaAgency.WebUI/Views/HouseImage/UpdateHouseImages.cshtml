@model UpdateHouseImageDto
@{
    Layout = null;
}

<head>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
</head>
<div class="content">
    <div class="container-fluid">
        <h4 class="page-title">Ev İşlemleri</h4>
        <div class="row">
            <div class="col-md-12">
                <form method="post">
                    <div class="card">
                        <div class="card-header">
                            <div class="card-title">Ev Resmi Güncelleme Sayfası</div>
                        </div>
                        <input type="hidden" asp-for="Id" />
                        
                        <div class="card-body">
                            <div class="form-group">
                                <label for="house">Evi Seçiniz</label>
                                <select id="house" asp-for="HouseId" class="form-control">
                                    <option value="">--Evi Seç--</option>
                                    @if (ViewBag.Houses != null)
                                    {
                                        @foreach (var item in ViewBag.Houses)
                                        {
                                            <option value="@item.Value">@item.Text</option>
                                        }
                                    }
                                </select>
                            </div>
                        </div>
                        <div class="card-body">
                            <div class="form-group">
                                <label for="image">Resmi Seçiniz</label>
                                <select id="image" asp-for="ImageId" class="form-control">
                                    <option value="">--Resmi Seç--</option>
                                    @if (ViewBag.Images != null)
                                    {
                                        @foreach (var user in ViewBag.Images)
                                        {
                                            <option value="@user.Value">@user.Text</option>
                                        }
                                    }
                                </select>
                            </div>
                        </div>

                        <div class="card-action">
                            <button class="btn btn-primary">Ev Resmini Güncelle</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>